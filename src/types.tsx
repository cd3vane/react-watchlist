export type Movie = {
    id: number,

    overview: string,
    poster_path: string,
    release_date: string, //yyyy-mm-dd
    runtime: number, // In minutes
    tagline: string
    title: string,
}